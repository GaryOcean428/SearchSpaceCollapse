node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Runtime data files (too large for GitHub, regenerated at runtime)
data/geometric-memory.json
data/tested-phrases-index.json
data/*.json

# Telemetry session logs (should use PostgreSQL, not local files)
qig-backend/logs/
*.jsonl

# Exclude draft documentation
docs/_drafts/*.md
.env

# Attached assets (temporary pasted content - not for version control)
attached_assets/*.txt
attached_assets/*.jpg
attached_assets/*.png
attached_assets/coordizer-32k-*/
