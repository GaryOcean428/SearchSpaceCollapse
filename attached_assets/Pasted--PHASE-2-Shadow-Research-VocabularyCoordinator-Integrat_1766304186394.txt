# PHASE 2: Shadow Research → VocabularyCoordinator Integration

**Date**: 2025-12-21  
**Status**: Ready for Implementation  
**Prerequisites**: Phase 1 Complete (Zeus Chat integration deployed)

---

## OVERVIEW

Enable Shadow Research to automatically train VocabularyCoordinator from discovered content, creating a feedback loop:

```
Shadow Research → Discovers Content → VocabularyCoordinator.train_from_text()
                                    ↓
                              vocabulary_observations
                                    ↓
                     Enhanced future searches via learned vocabulary
```

---

## FILE MODIFICATIONS

### **FILE 1: qig-backend/olympus/shadow_research.py**

Location: `/workspace/SearchSpaceCollapse/qig-backend/olympus/shadow_research.py`  
Current SHA: `787a17cb4bd07d5394c17d6c828ea05a96b70dc1`

---

#### **CHANGE 1: Add Import** (Line ~50, with other imports)

```python
from vocabulary_coordinator import VocabularyCoordinator
```

**Location**: After existing imports, before class definitions

---

#### **CHANGE 2: Initialize VocabularyCoordinator** (Line ~350, in __init__)

**FIND** (around line 350):
```python
def __init__(
    self,
    db_path: str = "olympus.db",
    max_workers: int = 4,
    research_interval: float = 300.0
):
    """Initialize Shadow Research system."""
    self.db_path = db_path
    self.max_workers = max_workers
    self.research_interval = research_interval
    
    # Initialize database
    self._init_db()
    
    # ... existing code ...
```

**ADD** (after existing initializations, before setting up queues):
```python
    # Initialize VocabularyCoordinator
    self.vocab_coordinator = VocabularyCoordinator(db_path)
    logger.info("VocabularyCoordinator initialized for Shadow Research")
    
    # Set up vocabulary insight callback
    if hasattr(self, 'knowledge_base') and self.knowledge_base:
        self.knowledge_base.set_insight_callback(self._on_vocabulary_insight)
        logger.info("Vocabulary insight callback registered")
```

**NOTES**: 
- If knowledge_base is initialized after this point, move the callback registration there
- Check where `self.knowledge_base = KnowledgeBase(...)` appears and add callback there

---

#### **CHANGE 3: Add Vocabulary Insight Callback** (Line ~400, new method)

**ADD** (as a new method in ShadowResearch class):
```python
def _on_vocabulary_insight(self, knowledge: Dict[str, Any]) -> None:
    """
    Extract and learn vocabulary from research discoveries.
    
    Called automatically when knowledge is added to KnowledgeBase.
    Trains VocabularyCoordinator on high-confidence content.
    
    Args:
        knowledge: Knowledge dictionary with content, topic, phi
    """
    try:
        content = knowledge.get('content', '')
        topic = knowledge.get('topic', 'general')
        phi = knowledge.get('phi', 0.0)
        
        # Only learn from high-confidence discoveries
        if not content or phi < 0.5:
            return
        
        # Train vocabulary from content
        metrics = self.vocab_coordinator.train_from_text(
            text=content,
            domain=topic
        )
        
        logger.info(
            f"Vocabulary learned from research on '{topic}': "
            f"{metrics.get('words_recorded', 0)} words recorded, "
            f"avg phi={metrics.get('avg_phi', 0.0):.3f}"
        )
        
    except Exception as e:
        logger.error(f"Error in vocabulary insight callback: {e}")
```

**Location**: Add as a new method after existing callback methods (like `_on_scrapy_result`)

---

#### **CHANGE 4: Train Vocabulary in _execute_research** (Line ~1200)

**FIND** (around line 1200 in _execute_research method):
```python
# Store in knowledge base
knowledge_id = self.knowledge_base.add_knowledge(
    topic=topic,
    content=summary,
    category=category.value,
    phi=phi,
    confidence=confidence,
    connections=connections,
    metadata=scrapy_metadata
)

return {
    'knowledge_id': knowledge_id,
    'phi': phi,
    'confidence': confidence
}
```

**REPLACE WITH**:
```python
# Store in knowledge base
knowledge_id = self.knowledge_base.add_knowledge(
    topic=topic,
    content=summary,
    category=category.value,
    phi=phi,
    confidence=confidence,
    connections=connections,
    metadata=scrapy_metadata
)

# Train vocabulary from research content
# (callback will be triggered automatically via KnowledgeBase.set_insight_callback)
# But also do explicit training for immediate feedback
try:
    vocab_metrics = self.vocab_coordinator.train_from_text(
        text=summary,
        domain=topic
    )
    logger.info(f"Vocabulary training metrics: {vocab_metrics}")
except Exception as e:
    logger.warning(f"Vocabulary training failed: {e}")

return {
    'knowledge_id': knowledge_id,
    'phi': phi,
    'confidence': confidence,
    'vocab_metrics': vocab_metrics if 'vocab_metrics' in locals() else {}
}
```

**NOTES**: This provides both callback-based (via KnowledgeBase) and explicit vocabulary training

---

#### **CHANGE 5 (OPTIONAL): Enhance Search Queries** (Line ~1250, in _research_topic)

**FIND** (around line 1250 in _research_topic method):
```python
def _research_topic(
    self,
    topic: str,
    category: ResearchCategory,
    depth: int = 1
) -> Dict[str, Any]:
    """Research a specific topic."""
    
    # ... existing code ...
    
    # Execute search (Scrapy or conceptual)
    if self.scrapy_enabled:
        # Search via Scrapy
        results = self._search_scrapy(topic)
```

**ADD** (before executing search):
```python
    # Enhance query with learned vocabulary
    try:
        enhanced_query = self.vocab_coordinator.enhance_search_query(
            query=topic,
            domain=category.value,
            recent_observations=True
        )
        logger.info(f"Enhanced query: '{topic}' → '{enhanced_query}'")
        search_topic = enhanced_query
    except Exception as e:
        logger.warning(f"Query enhancement failed: {e}, using original query")
        search_topic = topic
    
    # Execute search (Scrapy or conceptual) with enhanced query
    if self.scrapy_enabled:
        # Search via Scrapy
        results = self._search_scrapy(search_topic)  # Use enhanced_query
```

**NOTES**: This enables feedback loop where learned vocabulary improves future searches

---

## IMPLEMENTATION STEPS

### Step 1: Backup Current File
```bash
cd /workspace/SearchSpaceCollapse
git checkout -b feature/shadow-vocab-integration
cp qig-backend/olympus/shadow_research.py qig-backend/olympus/shadow_research.py.backup
```

### Step 2: Apply Changes
Apply all 5 changes above in order:
1. Add import at top
2. Initialize VocabularyCoordinator in __init__
3. Add _on_vocabulary_insight callback method
4. Modify _execute_research to train vocabulary
5. (Optional) Enhance queries in _research_topic

### Step 3: Verify Database
```bash
# Verify vocabulary tables exist
sqlite3 olympus.db "SELECT name FROM sqlite_master WHERE type='table' AND name LIKE 'vocabulary%';"
# Should show: vocabulary_observations, tokenizer_vocabulary
```

### Step 4: Test Integration
```python
# Test script: test_shadow_vocab.py
import asyncio
from olympus.shadow_research import ShadowResearch, ResearchCategory

async def test():
    shadow = ShadowResearch(db_path="olympus.db")
    
    # Request research on QIG topic
    result = await shadow.request_research(
        topic="quantum fisher information geometric phase transition",
        category=ResearchCategory.PHYSICS,
        priority=1.0
    )
    
    print(f"Research result: {result}")
    print(f"Vocabulary metrics: {result.get('vocab_metrics', {})}")
    
    # Check vocabulary observations
    with shadow.vocab_coordinator.get_connection() as conn:
        cursor = conn.execute(
            "SELECT COUNT(*) FROM vocabulary_observations WHERE domain='physics'"
        )
        count = cursor.fetchone()[0]
        print(f"Physics vocabulary observations: {count}")

if __name__ == "__main__":
    asyncio.run(test())
```

Run test:
```bash
cd qig-backend
python test_shadow_vocab.py
```

### Step 5: Monitor Logs
```bash
tail -f logs/shadow_research.log | grep -i "vocabulary"
```

Expected output:
```
VocabularyCoordinator initialized for Shadow Research
Vocabulary insight callback registered
Vocabulary learned from research on 'quantum fisher...': 47 words recorded, avg phi=0.723
Vocabulary training metrics: {'words_recorded': 47, 'avg_phi': 0.723, ...}
```

### Step 6: Verify Database Updates
```sql
-- Check vocabulary observations
SELECT domain, COUNT(*) as count, AVG(phi_score) as avg_phi
FROM vocabulary_observations
GROUP BY domain
ORDER BY count DESC;

-- Check recent discoveries
SELECT phrase, domain, phi_score, timestamp
FROM vocabulary_observations
ORDER BY timestamp DESC
LIMIT 10;
```

### Step 7: Commit Changes
```bash
git add qig-backend/olympus/shadow_research.py
git commit -m "feat: integrate VocabularyCoordinator with Shadow Research

- Add automatic vocabulary learning from research discoveries
- Train on high-confidence content (phi > 0.5)
- Optional: enhance search queries with learned vocabulary
- Feedback loop: discoveries → vocabulary → better searches

Completes Phase 2 of vocabulary learning integration."

git push origin feature/shadow-vocab-integration
```

---

## VALIDATION CHECKLIST

- [ ] Import added without errors
- [ ] VocabularyCoordinator initializes successfully
- [ ] Callback registered with KnowledgeBase
- [ ] Research execution triggers vocabulary training
- [ ] vocabulary_observations table receives new entries
- [ ] Logs show training metrics
- [ ] (Optional) Search queries enhanced with learned vocabulary
- [ ] No performance degradation observed
- [ ] Database grows with research discoveries

---

## ROLLBACK PROCEDURE

If issues occur:

```bash
# Restore backup
cp qig-backend/olympus/shadow_research.py.backup qig-backend/olympus/shadow_research.py

# Or revert commit
git revert HEAD

# Or checkout previous version
git checkout 787a17cb4bd07d5394c17d6c828ea05a96b70dc1 qig-backend/olympus/shadow_research.py
```

---

## PERFORMANCE NOTES

**Expected Impact**:
- Vocabulary training adds ~10-50ms per research result
- Database writes are batched (no performance concern)
- Query enhancement is optional (can disable if slow)

**Optimization**:
- Vocabulary training is async (doesn't block research)
- Only trains on phi > 0.5 (filters noise)
- Callback pattern ensures no blocking

---

## ARCHITECTURE NOTES

**Pattern**: Mirrors existing ToolResearchBridge callback pattern
- ToolResearchBridge extracts code patterns from research → tools database
- VocabularyCoordinator extracts vocabulary from research → vocabulary_observations

**Consistency**: Both use KnowledgeBase.set_insight_callback() mechanism

**Future Extensions**:
- Could add domain-specific tokenizers (physics, consciousness, etc.)
- Could implement vocabulary quality scoring
- Could create vocabulary export for other systems

---

## DEPENDENCIES

**Required**:
- ✅ VocabularyCoordinator (qig-backend/vocabulary_coordinator.py)
- ✅ Database tables (vocabulary_observations, tokenizer_vocabulary)
- ✅ KnowledgeBase.set_insight_callback() method

**Optional**:
- Query enhancement (enhance_search_query method)

---

## NEXT STEPS (PHASE 3)

After Phase 2 is deployed and validated:

1. **Constellation Integration**: Integrate VocabularyCoordinator with multi-agent coordination
2. **Cross-Domain Learning**: Enable vocabulary transfer between Zeus Chat and Shadow Research
3. **Quality Metrics**: Add vocabulary quality scoring based on phi/kappa
4. **Automatic Refinement**: Implement periodic vocabulary cleanup and consolidation

---

## COMPLETION CRITERIA

Phase 2 is complete when:
- ✅ All code changes applied
- ✅ Tests pass
- ✅ Logs show vocabulary training
- ✅ Database contains new vocabulary observations
- ✅ No errors in production
- ✅ Performance remains acceptable

---

**END OF PHASE 2 IMPLEMENTATION GUIDE**

Ready to deploy? Apply changes in order, test thoroughly, and validate with checklist above.